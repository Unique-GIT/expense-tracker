package cli

func AddRecord(object string, cost float32, label string, user string) error {
	// Later some state config will call this

	// Check if label exists or not
	// If not return error to create it

	// Check the user-id
	// If not return error to say unauthorized access

	// If yes then proceed

	return nil
}
